<?xml version="1.0" encoding="UTF-8"?>
<!--
    Empty include. The service registration logic moved into
    installer/wix/main.wxs (which overrides jpackage's bundled
    main.wxs) so we can put the Component directly inside Product.
    Kept as a no-op include since main.wxs still has the
    <?include $(var.JpConfigDir)/overrides.wxi ?> line.
-->
<Include>
</Include>
